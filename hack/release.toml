# commit to be tagged for the new release
commit = "HEAD"

project_name = "grpc-proxy"
github_repo = "siderolabs/grpc-proxy"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
